---
---

{% extends 'layouts/cover.njk' %}
{% block content %}
            <main class="px-3">
                <h1>James Belchamber</h1>
                <p class="lead"><strong>I like building things</strong></p>
                <p class="lead"><strong></strong></p>
                <p class="lead">
                    {%- for entry in site.navigation %}
                    {% if entry.label != "Home" %}
                    <a href="{{ entry.url }}" rel=me class="btn btn-secondary my-1">{{ entry.label }}</a>
                    {% endif %}
                    {%- endfor %}
                </p>
            </main>
{% endblock content %}
