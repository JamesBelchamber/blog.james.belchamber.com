{
  "name": "james.belchamber.com",
  "version": "1.0.0",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "@11ty/eleventy-img": "^2.0.1",
    "@11ty/eleventy-navigation": "^0.3.5",
    "bootstrap": "^5.2.0",
    "npm-run-all": "^4.1.5",
    "sass": "^1.54.0"
  },
  "scripts": {
    "watch:sass": "sass --watch _assets/sass:_site/css",
    "build:sass": "sass _assets/sass:_site/css",
    "watch:eleventy": "eleventy --serve",
    "build:eleventy": "eleventy",
    "start": "npm-run-all build:sass --parallel watch:*",
    "build": "npm-run-all build:*"
  }
}
